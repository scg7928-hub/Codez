{
  "version": 2,
  "buildCommand": "npm i -g pnpm@9 && pnpm install && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/codez run build",
  "outputDirectory": "frontend/dist/public",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index"
    }
  ],
  "env": {
    "BASE_PATH": "/",
    "NODE_ENV": "production"
  }
}
