{
  "name": "codez-development",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter @workspace/codez run dev",
    "build": "pnpm --filter @workspace/codez run build"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
